## Pattern negation

Various works on extension OCaml-like pattern matching with `not`-patterns. A pattern `not p` matches iff `p` does not match.

- [`docs/`](/docs/): paper, reports
- [`pmcomp/`](/pmcomp/): Implementation of pattern negation, compilation and exhaustivity with minimal toy language.
